var React = require('react-native');
var {
  StyleSheet,
  TouchableHighlight,
  View,
  Text
} = React;
var Spinner = require('../_spinner')

module.exports = React.createClass({

  render: function() {
    return(
      <Spinner />
    )
  }
});
